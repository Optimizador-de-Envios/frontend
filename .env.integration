# Sentinel file: loaded only by `npm run test:integration` via dotenv-cli.
# Enables the OpenRouteService integration test suite.
# Do NOT commit real API keys here — provide them in .env.local.
RUN_OPENROUTE_INTEGRATION=true
